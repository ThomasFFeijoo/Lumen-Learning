version: "3.2"

services:
    authorsapi:
        container_name: authorsapi
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 8090:8000
        volumes:
            - ../LumenAuthorsApi:/src

    booksapi:
        container_name: booksapi
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - 8091:8000
        volumes:
            - ../LumenBooksApi:/src